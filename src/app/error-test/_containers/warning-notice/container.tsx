import { WarningNoticePresentation } from "./presentational";

// Container Component: データフェッチとロジック
export async function WarningNoticeContainer() {
  // 静的なコンテンツのため特にデータフェッチは行わない

  return <WarningNoticePresentation />;
}
