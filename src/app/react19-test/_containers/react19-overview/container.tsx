import { React19OverviewPresentation } from "./presentational";

// Container Component: データフェッチとロジック
export async function React19OverviewContainer() {
  // 必要に応じてサーバーサイドでのデータ取得などを行う
  // この例では静的なコンテンツのため特に処理はなし

  return <React19OverviewPresentation />;
}
