import { N1VerificationPresentation } from "./presentational";

// Container Component: データフェッチとロジックを担当
export default function N1VerificationContainer() {
  // 将来的にはここでデータフェッチを行う
  // const data = await fetchData();

  return <N1VerificationPresentation />;
}
